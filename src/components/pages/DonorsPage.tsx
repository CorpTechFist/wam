import { Donors } from "../Donors";

export function DonorsPage() {
  return (
    <main className="py-5 relative overflow-hidden border-t border-b border-gray-200 border-opacity-50 bg-white">
      <div>
        <Donors />
      </div>
    </main>
  );
}
